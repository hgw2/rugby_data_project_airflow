formatted_area <- "data/formatted_area"

if (!dir.exists(formatted_area)){
  dir.create(formatted_area)
}

landing_area <- "data/landing_area"

if (!dir.exists(landing_area)){
  dir.create(landing_area)
}


